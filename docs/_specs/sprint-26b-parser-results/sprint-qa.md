# Sprint 26b — Sprint QA (Post-merge)

Filled after merge to `main`. Validates acceptance criteria from `spec.md` §6.

## Acceptance criteria validation
- [ ] `/` with rehydrated active lease renders `ParserResultsShell` (Mode B).
- [ ] PDF on the left; results stack on the right (Scan Timeline → Red Flags → Clauses → temporary chat).
- [ ] `ClausesList` shows one row per extracted clause; severity chip when graded.
- [ ] "View in PDF" works from red-flag cards AND clause rows.
- [ ] Header strip "Replace" resets the conversation (returns to Mode A).
- [ ] `AssistantFab.stub` still visible.
- [ ] All new components have passing colocated tests + the integration test passes.
- [ ] Playwright suite green including `parser-results.spec.ts`.
- [ ] Test count post-sprint ≥ pre-sprint.

## Evidence
- Screenshot of post-upload `/`: _attached_
- Lighthouse: deferred to Sprint 26d.
- axe-core: deferred to Sprint 26d.

## Follow-ups for next sprint (26c)
- _list anything from `impl-qa.md` that 26c should pick up_
